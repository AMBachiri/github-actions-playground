name: Deploy to poduction
on:
  workflow_dispatch:
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Show default variables
        run: |
          IMAGE_TAG=$(cut -c 1-8 <<< $GITHUB_SHA)
          echo $IMAGE_TAG
          env
